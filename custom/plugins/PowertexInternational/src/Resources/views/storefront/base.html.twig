{% sw_extends '@ComaxxTheme/storefront/base.html.twig' %}

{% block base_body %}
    {% set pricesAllowed = false %}

    {% if context.currentCustomerGroup.translated.customFields.powertex_customergroup_prices|default(false) %}
        {% set pricesAllowed = true %}
    {% endif %}

    {{ parent() }}
{% endblock %}
